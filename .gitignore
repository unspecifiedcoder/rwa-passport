# Foundry
out/
cache/
broadcast/

# Environment
.env
.env.local

# Dependencies (top-level only — don't ignore frontend/src/lib/)
/lib/
node_modules/

# Root package files (not needed — frontend has its own)
/package.json
/package-lock.json

# Frontend
frontend/node_modules/
frontend/.next/
frontend/.env.local

# ZK build artifacts (regenerate with circom + snarkjs)
src/zk/build/*.ptau
src/zk/build/*.r1cs
src/zk/build/*.wasm
src/zk/build/*_js/

# OS
.DS_Store

# IDE
.vscode/
.idea/
